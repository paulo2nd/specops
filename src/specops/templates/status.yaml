schema_version: 4
revision: 1
feature: "{{feature-name}}"
branch: "{{branch}}"
baseline: "{{commit-hash}}"
workflow_lane: "full"
active_artifact: "{{active-artifact}}"
created_at: "{{timestamp}}"
updated_at: "{{timestamp}}"

current_phase: "SPECIFY"

recovery:
  active_task: null
  last_commit: null
  blockers: []
  last_consistent_revision: 1
  last_consistent_at: "{{timestamp}}"
  migrated_from_backup: null

tasks: []

review_cycles: []

acknowledgements: []

workflow:
  skipped_steps: []
