"""Structured evidence records (Feature 012, US2).

The versioned, id-addressable successor to the flat ``<CLASS>:<summary>`` evidence
string: a record carrying a cache-key-derived id, producer, command, exit code,
timestamp, commit range, affected paths, summary, and an optional local-artifact
digest. Populated by US2 (T017); this stub reserves the module surface (T001).
"""
from __future__ import annotations

__all__: list[str] = []
