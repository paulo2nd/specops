# Minimal valid gate-profile config fixture (Feature 021 frozen-shape test).
# A single profile with only the required fields; timeout/required/applies default.
profiles:
  - name: test
    command: pytest -q
